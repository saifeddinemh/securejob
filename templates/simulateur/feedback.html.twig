{% extends 'base.html.twig' %}

{% block title %}Feedback IA - Simulateur SecureJob{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .ai-container {
            background: linear-gradient(135deg, #ffffff, #f8f9fa);
            border-radius: 20px;
            padding: 35px;
            box-shadow: 0 15px 35px rgba(0, 150, 136, 0.1);
            border: 3px solid #00b894;
        }
        .ai-header {
            background: linear-gradient(135deg, #00b894, #00a085);
            color: white;
            padding: 25px;
            border-radius: 15px;
            margin-bottom: 30px;
            text-align: center;
        }
        .textarea-analyse {
            border: 3px solid #8e2de2;
            border-radius: 15px;
            padding: 20px;
            min-height: 200px;
            resize: vertical;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }
        .textarea-analyse:focus {
            box-shadow: 0 0 0 5px rgba(142, 45, 226, 0.2);
            border-color: #4a00e0;
        }
        .stats-card {
            background: white;
            border-radius: 12px;
            padding: 20px;
            margin: 15px 0;
            border-left: 5px solid #2196f3;
            box-shadow: 0 3px 10px rgba(0,0,0,0.05);
        }
        .score-circle-small {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1.2rem;
            margin: 0 auto 10px;
        }
        .score-structure { background: #e3f2fd; color: #1565c0; }
        .score-contenu { background: #f3e5f5; color: #7b1fa2; }
        .score-clarte { background: #e8f5e9; color: #2e7d32; }
        .score-global { background: linear-gradient(135deg, #8e2de2, #4a00e0); color: white; }
        .point-fort-box {
            background: linear-gradient(135deg, #e8f5e9, #c8e6c9);
            border-radius: 10px;
            padding: 20px;
            margin: 15px 0;
            border-left: 5px solid #4caf50;
        }
        .suggestion-box {
            background: linear-gradient(135deg, #fff3e0, #ffe0b2);
            border-radius: 10px;
            padding: 20px;
            margin: 15px 0;
            border-left: 5px solid #ff9800;
        }
        .mots-cles {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: 10px;
        }
        .mot-cle {
            background: #8e2de2;
            color: white;
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
        }
        .progress-bar-custom {
            height: 20px;
            border-radius: 10px;
            background: #e0e0e0;
            overflow: hidden;
            margin: 10px 0;
        }
        .progress-fill {
            height: 100%;
            border-radius: 10px;
            background: linear-gradient(90deg, #00b894, #00897b);
        }
        .chat-bubble {
            background: #f5f5f5;
            border-radius: 20px;
            padding: 15px 20px;
            margin: 15px 0;
            max-width: 80%;
            position: relative;
        }
        .chat-bubble.ai {
            background: #e3f2fd;
            margin-left: auto;
        }
        .chat-bubble::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 20px;
            border-width: 10px 10px 0;
            border-style: solid;
            border-color: #f5f5f5 transparent transparent;
        }
        .chat-bubble.ai::after {
            left: auto;
            right: 20px;
            border-color: #e3f2fd transparent transparent;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="ai-header">
            <h1 class="display-5">🤖 Analyse Intelligente par IA</h1>
            <p class="lead mb-0">Notre intelligence artificielle analyse vos réponses et vous donne un feedback détaillé</p>
        </div>

        {% if not analyse %}
            <!-- Formulaire d'analyse -->
            <div class="ai-container">
                <div class="row">
                    <div class="col-md-8">
                        <h3 style="color: #4a00e0;">📝 Soumettez une réponse à analyser</h3>
                        <p>Collez ou écrivez une réponse d'entretien que vous souhaitez faire analyser par notre IA :</p>

                        <form method="post">
                            <div class="mb-4">
                                <label class="form-label fw-bold">
                                    Votre réponse (minimum 100 caractères recommandé) :
                                </label>
                                <textarea name="texte_reponse"
                                          class="form-control textarea-analyse"
                                          rows="10"
                                          placeholder="Exemple : 'Pour gérer un conflit en équipe, je commencerais par organiser une réunion privée avec les personnes concernées. Mon approche serait d'écouter chaque point de vue sans jugement, d'identifier les causes racines du conflit, et de proposer des solutions collaboratives...'"
                                          required></textarea>
                                <div class="text-end mt-2">
                                    <small class="text-muted">Caractères : <span id="charCount">0</span></small>
                                </div>
                            </div>

                            <!-- Conseils de rédaction -->
                            <div class="alert alert-light mb-4">
                                <h6>💡 Pour une analyse optimale :</h6>
                                <ul class="mb-0">
                                    <li>Écrivez au moins 100-200 caractères</li>
                                    <li>Structurez votre réponse (introduction, développement, conclusion)</li>
                                    <li>Utilisez des termes techniques appropriés</li>
                                    <li>Donnez des exemples concrets</li>
                                </ul>
                            </div>

                            <!-- Bouton d'analyse -->
                            <div class="text-center">
                                <button type="submit" class="btn btn-custom btn-lg" style="padding: 15px 50px;">
                                    🧠 Analyser avec l'IA
                                </button>
                                <p class="text-muted mt-2 small">
                                    L'analyse prend quelques secondes...
                                </p>
                            </div>
                        </form>
                    </div>

                    <div class="col-md-4">
                        <div class="stats-card">
                            <h5 style="color: #00b894;">📊 Ce que l'IA analyse</h5>
                            <ul class="small">
                                <li><strong>Structure</strong> : Organisation des idées</li>
                                <li><strong>Contenu</strong> : Pertinence technique</li>
                                <li><strong>Clarté</strong> : Lisibilité et concision</li>
                                <li><strong>Vocabulaire</strong> : Termes professionnels</li>
                                <li><strong>Exemples</strong> : Cas concrets cités</li>
                            </ul>
                        </div>

                        <div class="stats-card">
                            <h5 style="color: #8e2de2;">🎯 Exemples d'analyse</h5>
                            <div class="chat-bubble">
                                <strong>Réponse faible :</strong> "Je parlerais avec les gens."
                            </div>
                            <div class="chat-bubble ai">
                                <strong>IA :</strong> "Trop vague. Ajoutez des détails sur la méthode."
                            </div>
                            <div class="chat-bubble">
                                <strong>Réponse forte :</strong> "J'utiliserais la méthode XYZ avec étapes A, B, C..."
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <!-- Résultats de l'analyse -->
            <div class="ai-container">
                <div class="text-center mb-5">
                    <h2 style="color: #4a00e0;">📈 Rapport d'analyse IA</h2>
                    <p class="lead">Évaluation détaillée de votre réponse</p>
                </div>

                <!-- Scores -->
                <div class="row text-center mb-5">
                    <div class="col-md-3">
                        <div class="score-circle-small score-structure">
                            {{ analyse.scores.structure }}%
                        </div>
                        <div>Structure</div>
                    </div>
                    <div class="col-md-3">
                        <div class="score-circle-small score-contenu">
                            {{ analyse.scores.contenu }}%
                        </div>
                        <div>Contenu</div>
                    </div>
                    <div class="col-md-3">
                        <div class="score-circle-small score-clarte">
                            {{ analyse.scores.clarte }}%
                        </div>
                        <div>Clarté</div>
                    </div>
                    <div class="col-md-3">
                        <div class="score-circle-small score-global">
                            {{ analyse.scores.global }}%
                        </div>
                        <div>Score global</div>
                    </div>
                </div>

                <!-- Statistiques -->
                <div class="stats-card">
                    <h5 style="color: #2196f3;">📊 Statistiques de la réponse</h5>
                    <div class="row mt-3">
                        <div class="col-md-6">
                            <strong>Longueur :</strong> {{ analyse.statistiques.longueur }}<br>
                            <strong>Nombre de phrases :</strong> {{ analyse.statistiques.phrases }}<br>
                            <strong>Mots par phrase :</strong> {{ analyse.statistiques.moyenne_mots_phrase }}
                        </div>
                        <div class="col-md-6">
                            <strong>Mots pertinents détectés :</strong> {{ analyse.statistiques.mots_pertinents }}<br>
                            <strong>Évaluation globale :</strong> {{ analyse.evaluation }}<br>
                            {% if analyse.mots_cles_trouves|length > 0 %}
                                <div class="mots-cles">
                                    {% for mot in analyse.mots_cles_trouves %}
                                        <span class="mot-cle">{{ mot }}</span>
                                    {% endfor %}
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>

                <!-- Points forts -->
                {% if analyse.points_forts|length > 0 %}
                    <div class="point-fort-box">
                        <h5 style="color: #4caf50;">✅ Points forts identifiés</h5>
                        <ul class="mb-0">
                            {% for point in analyse.points_forts %}
                                <li>{{ point }}</li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}

                <!-- Suggestions d'amélioration -->
                {% if analyse.suggestions|length > 0 %}
                    <div class="suggestion-box">
                        <h5 style="color: #ff9800;">💡 Suggestions d'amélioration</h5>
                        <ul class="mb-0">
                            {% for suggestion in analyse.suggestions %}
                                <li>{{ suggestion }}</li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}

                <!-- Conversation avec l'IA -->
                <div class="mt-5">
                    <h4 style="color: #8e2de2;">💬 Dialogue avec l'IA</h4>

                    <div class="chat-bubble">
                        <strong>Vous :</strong> "Comment puis-je améliorer cette réponse ?"
                    </div>

                    <div class="chat-bubble ai">
                        <strong>IA :</strong> "Voici mon analyse détaillée :<br><br>
                        1. <strong>Structure</strong> : {{ analyse.scores.structure }}% - {{ analyse.scores.structure >= 70 ? 'Bonne organisation' : 'À mieux structurer' }}<br>
                        2. <strong>Contenu</strong> : {{ analyse.scores.contenu }}% - {{ analyse.scores.contenu >= 70 ? 'Pertinent' : 'À enrichir' }}<br>
                        3. <strong>Clarté</strong> : {{ analyse.scores.clarte }}% - {{ analyse.scores.clarte >= 70 ? 'Clair' : 'À clarifier' }}<br><br>
                        {{ analyse.suggestions|first|default('Continuez à pratiquer !') }}"
                    </div>

                    <div class="chat-bubble">
                        <strong>Vous :</strong> "Que faire concrètement pour progresser ?"
                    </div>

                    <div class="chat-bubble ai">
                        <strong>IA :</strong> "1. <strong>Pratiquez</strong> : Écrivez 3 réponses par semaine<br>
                        2. <strong>Structurez</strong> : Utilisez le modèle Introduction-Développement-Conclusion<br>
                        3. <strong>Enrichissez</strong> : Ajoutez toujours un exemple concret<br>
                        4. <strong>Relisez</strong> : Vérifiez la longueur (100-200 mots idéal)"
                    </div>
                </div>

                <!-- Actions -->
                <div class="text-center mt-5">
                    <a href="{{ path('simulateur_feedback') }}" class="btn btn-custom btn-lg me-3">
                        🔁 Analyser une autre réponse
                    </a>
                    <a href="{{ path('simulateur_comportemental') }}" class="btn btn-custom btn-lg">
                        🎭 Pratiquer des scénarios
                    </a>
                </div>
            </div>
        {% endif %}
    </div>

    <script>
        // Compteur de caractères
        const textarea = document.querySelector('textarea[name="texte_reponse"]');
        const charCount = document.getElementById('charCount');

        if (textarea && charCount) {
            textarea.addEventListener('input', function() {
                const length = this.value.length;
                charCount.textContent = length;

                // Changer la couleur selon la longueur
                if (length < 50) {
                    charCount.style.color = '#f44336';
                } else if (length < 100) {
                    charCount.style.color = '#ff9800';
                } else if (length < 200) {
                    charCount.style.color = '#4caf50';
                } else {
                    charCount.style.color = '#2e7d32';
                }
            });

            // Initialiser le compteur
            charCount.textContent = textarea.value.length;
        }
    </script>
{% endblock %}
