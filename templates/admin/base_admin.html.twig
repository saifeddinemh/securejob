<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}SecureJob Admin{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    {% block stylesheets %}
        <style>
            body {
                font-family: 'Segoe UI', sans-serif;
                background: #f8f9fa;
                min-height: 100vh;
                display: flex;
                flex-direction: column;
            }
            .navbar-admin {
                background: linear-gradient(90deg, #1a237e, #283593);
                color: #fff;
                box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            }
            .navbar-admin .navbar-brand {
                color: #fff;
                font-weight: 700;
                font-size: 1.5rem;
            }
            .navbar-admin .nav-link {
                color: rgba(255,255,255,0.8);
                font-weight: 500;
                transition: color 0.3s;
            }
            .navbar-admin .nav-link:hover, .navbar-admin .nav-link.active {
                color: #fff;
            }
            .sidebar {
                background: #fff;
                box-shadow: 2px 0 10px rgba(0,0,0,0.05);
                min-height: calc(100vh - 70px);
                padding-top: 2rem;
            }
            .sidebar .nav-link {
                color: #555;
                padding: 0.8rem 1.5rem;
                display: flex;
                align-items: center;
                gap: 10px;
                transition: all 0.2s;
            }
            .sidebar .nav-link:hover, .sidebar .nav-link.active {
                background: #f0f2f5;
                color: #1a237e;
                border-left: 4px solid #1a237e;
            }
            .card-stat {
                border: none;
                border-radius: 12px;
                box-shadow: 0 4px 15px rgba(0,0,0,0.05);
                transition: transform 0.3s;
            }
            .card-stat:hover {
                transform: translateY(-5px);
            }
            .card-icon {
                width: 50px;
                height: 50px;
                border-radius: 10px;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 1.5rem;
                color: #fff;
            }
            .bg-gradient-primary { background: linear-gradient(135deg, #4facfe, #00f2fe); }
            .bg-gradient-success { background: linear-gradient(135deg, #43e97b, #38f9d7); }
            .bg-gradient-warning { background: linear-gradient(135deg, #fccb90, #d57eeb); }
            .bg-gradient-info { background: linear-gradient(135deg, #a18cd1, #fbc2eb); }
        </style>
    {% endblock %}
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-admin">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('admin_dashboard') }}">
            <i class="fas fa-shield-alt me-2"></i>SecureJob Admin
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('candidat_index') }}"><i class="fas fa-home me-1"></i> Site Public</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-danger" href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt me-1"></i> Déconnexion</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container-fluid flex-grow-1">
    <div class="row h-100">
        <div class="col-md-3 col-lg-2 sidebar d-none d-md-block">
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.get('_route') == 'admin_dashboard' %}active{% endif %}" href="{{ path('admin_dashboard') }}">
                        <i class="fas fa-tachometer-alt"></i> Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.get('_route') == 'admin_recruiters' %}active{% endif %}" href="{{ path('admin_recruiters') }}">
                        <i class="fas fa-building"></i> Recruteurs
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.get('_route') == 'admin_candidates' %}active{% endif %}" href="{{ path('admin_candidates') }}">
                        <i class="fas fa-users"></i> Candidats
                    </a>
                </li>
            </ul>
        </div>
        <div class="col-md-9 col-lg-10 p-4">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
